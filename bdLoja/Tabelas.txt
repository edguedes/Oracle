CREATE TABLE CLIENTE
(
cli__id INT NOT NULL,
cli_nome VARCHAR2(50) NOT NULL,
cli_cidade VARCHAR2(30)
);

== ALTERANDO TABELA CLIENTES E INSERINDO UMA CHAVE PRIMARIA --
ALTER TABLE CLIENTE ADD CONSTRAINT  IDCLIENTE PRIMARY KEY (IDCLIENTE);
ALTER TABLE CLIENTE ADD cli_uf char(2);
ALTER TABLE CLIENTE MODIFY cli_uf VARCHAR (10);
ALTER TABLE CLIENTE MODIFY cli_uf CHAR (2);

CREATE TABLE PRODUTO
(
prod_id int NOT NULL,
prod_nome varchar2(30) NOT NULL,
prod_preco numeric(8,2) NOT NULL,

-- CRIANDO JA COM PRIMARY KEY
CONSTRAINT  PK_PROD_ID PRIMARY KEY (PROD_ID)
);

-- DESCREVE A ESTRUTURA DA TABELA --
DESCRIBE PRODUTO;

-- UPDATE PARA ATUALIZAR A  TODOS OS CAMPOS CIDADE DA TABELA --
UPDATE CLIENTE SET CLI_CIDADE = 'CARUARU';

-- UPDATE PARA ATUALIZAR O CAMPO CIDADE DO CLIENTE CODIGO 3 --
UPDATE CLIENTE SET CLI_CIDADE = 'ARARAS' WHERE CLI_CODIGO = 3;

-- EXCLUINDO UM REGISTRO POR PARAMETRO --
delete from cliente where cli_id = 3;

-- EXCLUINDO UM REGISTRO POR INTERVALO --
delete from cliente where cli_id BETWEEN 1 AND 4;